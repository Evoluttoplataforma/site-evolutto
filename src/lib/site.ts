/** URL canônica do site (sempre com www — evita redirect errado do /blog no apex). */
export const SITE_ORIGIN = 'https://www.evolutto.com';
export const BLOG_HOME = `${SITE_ORIGIN}/blog/`;
export const CTA_URL = `${SITE_ORIGIN}/#cta`;
